import { useEffect, useMemo, useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import {
  Alert, Box, Button, Card, Checkbox, Chip, Dialog, DialogActions, DialogContent,
  DialogTitle, Divider, FormControlLabel, IconButton, MenuItem, Skeleton, Stack,
  TextField, Tooltip, Typography,
} from '@mui/material';
import {
  AddRounded, AssignmentTurnedInOutlined, ChatBubbleOutlineRounded, EditOutlined,
  PersonOutlineRounded, PlayArrowRounded, SendRounded, TaskAltOutlined,
} from '@mui/icons-material';
import { api, ApiError } from '../../api/client';
import type { DossierAssignment, PagedResponse, TaskComment, WorkTask } from '../../types/api';
import { formatDate, taskPriorityLabels, taskStatusLabels } from './options';

const today = new Date().toISOString().slice(0, 10);

function statusColor(status: string): 'default' | 'primary' | 'success' | 'warning' {
  if (status === 'TERMINEE') return 'success';
  if (status === 'PRETE_POUR_REVISION') return 'warning';
  if (status === 'EN_COURS') return 'primary';
  return 'default';
}

interface TaskFormValues { title: string; description: string; dueOn: string; priority: string; checklist: string }

function TaskFormDialog({ open, onClose, organizationId, dossierId, task }: { open: boolean; onClose: () => void; organizationId: string; dossierId: string; task?: WorkTask | null }) {
  const queryClient = useQueryClient();
  const [values, setValues] = useState<TaskFormValues>({ title: '', description: '', dueOn: today, priority: 'NORMALE', checklist: '' });
  const [error, setError] = useState('');
  const editing = Boolean(task);
  useEffect(() => {
    if (!open) return;
    setValues(task ? { title: task.title, description: task.description ?? '', dueOn: task.dueOn, priority: task.priority, checklist: '' } : { title: '', description: '', dueOn: today, priority: 'NORMALE', checklist: '' });
    setError('');
  }, [open, task]);
  const mutation = useMutation({
    mutationFn: () => {
      if (!values.title.trim()) throw new Error('Le titre est obligatoire.');
      const payload = { title: values.title.trim(), description: values.description.trim() || null, dueOn: values.dueOn, priority: values.priority, ...(editing ? {} : { checklist: values.checklist.split('\n').map((line) => line.trim()).filter(Boolean) }) };
      return editing ? api.patch<WorkTask>(`/api/organizations/${organizationId}/dossiers/${dossierId}/tasks/${task?.id}`, payload) : api.post<WorkTask>(`/api/organizations/${organizationId}/dossiers/${dossierId}/tasks`, payload);
    },
    onSuccess: async () => { await queryClient.invalidateQueries({ queryKey: ['dossier-tasks', organizationId, dossierId] }); onClose(); },
    onError: (reason) => setError(reason instanceof ApiError || reason instanceof Error ? reason.message : 'Impossible d’enregistrer la tâche.'),
  });
  return <Dialog open={open} onClose={mutation.isPending ? undefined : onClose} fullWidth maxWidth="sm"><DialogTitle><Typography variant="h3" sx={{ fontSize: 28 }}>{editing ? 'Modifier la tâche' : 'Créer une tâche'}</Typography></DialogTitle><DialogContent dividers>{error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}<Box sx={{ display: 'grid', gap: 2 }}><TextField label="Titre" value={values.title} onChange={(event) => setValues({ ...values, title: event.target.value })} required /><TextField label="Description" multiline minRows={3} value={values.description} onChange={(event) => setValues({ ...values, description: event.target.value })} /><Box sx={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 2 }}><TextField type="date" label="Échéance" value={values.dueOn} onChange={(event) => setValues({ ...values, dueOn: event.target.value })} slotProps={{ inputLabel: { shrink: true } }} /><TextField select label="Priorité" value={values.priority} onChange={(event) => setValues({ ...values, priority: event.target.value })}>{Object.entries(taskPriorityLabels).map(([value, label]) => <MenuItem key={value} value={value}>{label}</MenuItem>)}</TextField></Box>{!editing && <TextField label="Checklist initiale" multiline minRows={4} value={values.checklist} onChange={(event) => setValues({ ...values, checklist: event.target.value })} helperText="Un élément par ligne." />}</Box></DialogContent><DialogActions><Button onClick={onClose}>Annuler</Button><Button variant="contained" onClick={() => mutation.mutate()} disabled={mutation.isPending}>{mutation.isPending ? 'Enregistrement…' : 'Enregistrer'}</Button></DialogActions></Dialog>;
}

function TaskDetailDialog({ task, open, onClose, organizationId, dossierId, assignments, canManage, canAssign, canValidate }: { task: WorkTask | null; open: boolean; onClose: () => void; organizationId: string; dossierId: string; assignments: DossierAssignment[]; canManage: boolean; canAssign: boolean; canValidate: boolean }) {
  const queryClient = useQueryClient();
  const [newItem, setNewItem] = useState('');
  const [comment, setComment] = useState('');
  const [rejectComment, setRejectComment] = useState('');
  const [rejectOpen, setRejectOpen] = useState(false);
  const [editOpen, setEditOpen] = useState(false);
  const [error, setError] = useState('');
  const comments = useQuery({ queryKey: ['task-comments', organizationId, dossierId, task?.id], queryFn: () => api.get<TaskComment[]>(`/api/organizations/${organizationId}/dossiers/${dossierId}/tasks/${task?.id}/comments`), enabled: Boolean(open && task?.id) });
  const refresh = async () => { await queryClient.invalidateQueries({ queryKey: ['dossier-tasks', organizationId, dossierId] }); await queryClient.invalidateQueries({ queryKey: ['task-comments', organizationId, dossierId, task?.id] }); };
  const action = useMutation({ mutationFn: async ({ type, payload }: { type: string; payload?: unknown }) => {
    if (!task) return;
    const base = `/api/organizations/${organizationId}/dossiers/${dossierId}/tasks/${task.id}`;
    if (type === 'start') return api.patch(base + '/progress', { status: 'EN_COURS' });
    if (type === 'submit') return api.patch(base + '/progress', { status: 'PRETE_POUR_REVISION', comment: comment.trim() || null });
    if (type === 'complete') return api.post(base + '/complete');
    if (type === 'reject') return api.post(base + '/reject', { comment: rejectComment.trim() });
    if (type.startsWith('assign:')) return api.put(base + `/assignee/${type.slice(7)}`);
    if (type.startsWith('check:')) return api.patch(base + `/checklist/${type.slice(6)}`, payload);
    if (type === 'add-check') return api.post(base + '/checklist', { label: newItem.trim() });
    if (type === 'comment') return api.post(base + '/comments', { body: comment.trim() });
  }, onSuccess: async (_, variables) => { setError(''); if (variables.type === 'add-check') setNewItem(''); if (variables.type === 'comment' || variables.type === 'submit') setComment(''); if (variables.type === 'reject') { setRejectComment(''); setRejectOpen(false); } await refresh(); }, onError: (reason) => setError(reason instanceof ApiError ? reason.message : 'Action impossible.') });
  if (!task) return null;
  const closed = ['TERMINEE', 'ANNULEE'].includes(task.status);
  return <><Dialog open={open} onClose={onClose} fullWidth maxWidth="md"><DialogTitle sx={{ pb: 1 }}><Box sx={{ display: 'flex', gap: 2, alignItems: 'flex-start' }}><Box sx={{ flex: 1 }}><Typography variant="overline" color="secondary.main" sx={{ fontWeight: 800 }}>{task.type === 'OBLIGATION' ? 'Tâche fiscale' : 'Tâche manuelle'}</Typography><Typography variant="h3" sx={{ fontSize: 29 }}>{task.title}</Typography><Stack direction="row" spacing={1} sx={{ mt: 1, flexWrap: 'wrap' }}><Chip label={taskStatusLabels[task.status] ?? task.status} color={statusColor(task.status)} size="small" /><Chip label={taskPriorityLabels[task.priority] ?? task.priority} size="small" variant="outlined" /><Chip label={formatDate(task.dueOn)} size="small" color={task.isOverdue ? 'error' : 'default'} variant="outlined" /></Stack></Box>{canManage && !closed && <Tooltip title="Modifier"><IconButton onClick={() => setEditOpen(true)}><EditOutlined /></IconButton></Tooltip>}</Box></DialogTitle><DialogContent dividers>{error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}<Box className="task-detail-grid"><Stack spacing={2.5}><Box><Typography sx={{ fontWeight: 800, mb: .5 }}>Description</Typography><Typography color="text.secondary" sx={{ whiteSpace: 'pre-wrap' }}>{task.description || 'Aucune description.'}</Typography></Box><Box><Box sx={{ display: 'flex', justifyContent: 'space-between', mb: 1 }}><Typography sx={{ fontWeight: 800 }}>Checklist</Typography><Typography variant="body2" color="text.secondary">{task.checklistCompleted}/{task.checklistTotal}</Typography></Box>{task.checklist.map((item) => <FormControlLabel key={item.id} sx={{ display: 'flex', mx: 0 }} control={<Checkbox checked={item.isCompleted} disabled={!canManage || closed || action.isPending} onChange={(_, checked) => action.mutate({ type: `check:${item.id}`, payload: { isCompleted: checked } })} />} label={<Typography sx={{ textDecoration: item.isCompleted ? 'line-through' : 'none', color: item.isCompleted ? 'text.secondary' : 'text.primary' }}>{item.label}</Typography>} />)}{canManage && !closed && <Box sx={{ display: 'flex', gap: 1, mt: 1 }}><TextField size="small" fullWidth placeholder="Nouvel élément…" value={newItem} onChange={(event) => setNewItem(event.target.value)} /><Button onClick={() => action.mutate({ type: 'add-check' })} disabled={!newItem.trim() || action.isPending}>Ajouter</Button></Box>}</Box><Divider /><Box><Typography sx={{ fontWeight: 800, mb: 1 }}>Commentaires</Typography>{comments.isLoading && <Skeleton height={80} />}{comments.data?.map((entry) => <Box key={entry.id} sx={{ p: 1.5, mb: 1, bgcolor: '#f7f5ef', borderRadius: 2 }}><Typography variant="caption" sx={{ fontWeight: 800 }}>{entry.authorName ?? 'Utilisateur'} · {new Intl.DateTimeFormat('fr-TN', { dateStyle: 'short', timeStyle: 'short' }).format(new Date(entry.createdAtUtc))}</Typography><Typography variant="body2" sx={{ mt: .5, whiteSpace: 'pre-wrap' }}>{entry.body}</Typography></Box>)}{canManage && <Box sx={{ display: 'flex', gap: 1, mt: 1 }}><TextField size="small" fullWidth multiline maxRows={4} placeholder="Ajouter un commentaire…" value={comment} onChange={(event) => setComment(event.target.value)} /><IconButton color="primary" onClick={() => action.mutate({ type: 'comment' })} disabled={!comment.trim()}><SendRounded /></IconButton></Box>}</Box></Stack><Stack spacing={2}><Card variant="outlined"><Box sx={{ p: 2 }}><Typography variant="caption" color="text.secondary">Collaborateur</Typography><Typography sx={{ fontWeight: 800 }}>{task.assigneeName || 'Non affectée'}</Typography>{canAssign && !closed && <TextField select size="small" fullWidth label="Affecter à" value={task.assigneeMembershipId ?? ''} onChange={(event) => action.mutate({ type: `assign:${event.target.value}` })} sx={{ mt: 2 }}>{assignments.filter((entry) => entry.isActive).map((entry) => <MenuItem key={entry.membershipId} value={entry.membershipId}>{entry.fullName}</MenuItem>)}</TextField>}</Box></Card><Card variant="outlined"><Box sx={{ p: 2 }}><Typography sx={{ fontWeight: 800, mb: 1.5 }}>Workflow</Typography><Stack spacing={1}>{canManage && task.status === 'A_FAIRE' && <Button variant="contained" startIcon={<PlayArrowRounded />} onClick={() => action.mutate({ type: 'start' })}>Démarrer</Button>}{canManage && task.status === 'EN_COURS' && <Button variant="contained" startIcon={<SendRounded />} onClick={() => action.mutate({ type: 'submit' })}>Soumettre pour révision</Button>}{canValidate && task.status === 'PRETE_POUR_REVISION' && <Button variant="contained" color="success" startIcon={<AssignmentTurnedInOutlined />} onClick={() => action.mutate({ type: 'complete' })}>Valider la tâche</Button>}{canValidate && task.status === 'PRETE_POUR_REVISION' && <Button color="error" variant="outlined" onClick={() => setRejectOpen(true)}>Renvoyer en correction</Button>}{closed && <Alert severity="success">Cette tâche est clôturée.</Alert>}</Stack></Box></Card>{task.lastComment && <Alert severity="info">{task.lastComment}</Alert>}</Stack></Box></DialogContent><DialogActions><Button onClick={onClose}>Fermer</Button></DialogActions></Dialog><TaskFormDialog open={editOpen} onClose={() => setEditOpen(false)} organizationId={organizationId} dossierId={dossierId} task={task} /><Dialog open={rejectOpen} onClose={() => setRejectOpen(false)} fullWidth maxWidth="xs"><DialogTitle>Renvoyer la tâche</DialogTitle><DialogContent><TextField autoFocus fullWidth multiline minRows={3} label="Motif obligatoire" value={rejectComment} onChange={(event) => setRejectComment(event.target.value)} /></DialogContent><DialogActions><Button onClick={() => setRejectOpen(false)}>Annuler</Button><Button color="error" variant="contained" disabled={!rejectComment.trim()} onClick={() => action.mutate({ type: 'reject' })}>Renvoyer</Button></DialogActions></Dialog></>;
}

export function DossierTasksPanel({ organizationId, dossierId, assignments, archived, canManage, canAssign, canValidate }: { organizationId: string; dossierId: string; assignments: DossierAssignment[]; archived: boolean; canManage: boolean; canAssign: boolean; canValidate: boolean }) {
  const [status, setStatus] = useState('');
  const [formOpen, setFormOpen] = useState(false);
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const query = useQuery({ queryKey: ['dossier-tasks', organizationId, dossierId, status], queryFn: () => api.get<PagedResponse<WorkTask>>(`/api/organizations/${organizationId}/dossiers/${dossierId}/tasks?page=1&pageSize=100${status ? `&status=${status}` : ''}`) });
  const selected = useMemo(() => query.data?.items.find((item) => item.id === selectedId) ?? null, [query.data, selectedId]);
  return <><Card><Box sx={{ p: 2.5, display: 'flex', gap: 2, alignItems: 'center', justifyContent: 'space-between', flexWrap: 'wrap' }}><Box><Typography variant="h3" sx={{ fontSize: 24 }}>Tâches du dossier</Typography><Typography variant="body2" color="text.secondary">Checklist, affectation et circuit de validation.</Typography></Box><Stack direction="row" spacing={1}><TextField select size="small" label="Statut" value={status} onChange={(event) => setStatus(event.target.value)} sx={{ minWidth: 180 }}><MenuItem value="">Tous</MenuItem>{Object.entries(taskStatusLabels).map(([value, label]) => <MenuItem key={value} value={value}>{label}</MenuItem>)}</TextField>{canManage && !archived && <Button variant="contained" startIcon={<AddRounded />} onClick={() => setFormOpen(true)}>Nouvelle tâche</Button>}</Stack></Box>{query.isError && <Alert severity="error" sx={{ mx: 2.5, mb: 2 }}>Impossible de charger les tâches.</Alert>}{query.isLoading && <Box sx={{ p: 2.5 }}><Skeleton height={80} /><Skeleton height={80} /></Box>}{!query.isLoading && !query.data?.items.length && <Box sx={{ p: 6, textAlign: 'center' }}><TaskAltOutlined sx={{ fontSize: 42, color: 'text.disabled' }} /><Typography sx={{ fontWeight: 800, mt: 1 }}>Aucune tâche</Typography><Typography variant="body2" color="text.secondary">Créez une tâche ou générez les obligations fiscales.</Typography></Box>}{query.data?.items.map((task) => <Box key={task.id} onClick={() => setSelectedId(task.id)} sx={{ px: 3, py: 2, borderTop: '1px solid', borderColor: 'divider', display: 'flex', alignItems: 'center', gap: 2, cursor: 'pointer', '&:hover': { bgcolor: '#faf8f2' } }}><Box sx={{ width: 40, height: 40, borderRadius: 2.5, bgcolor: task.isOverdue ? '#fdeaea' : '#e4efe9', color: task.isOverdue ? 'error.main' : 'primary.main', display: 'grid', placeItems: 'center' }}>{task.type === 'OBLIGATION' ? <AssignmentTurnedInOutlined /> : <TaskAltOutlined />}</Box><Box sx={{ flex: 1, minWidth: 0 }}><Typography sx={{ fontWeight: 800 }} noWrap>{task.title}</Typography><Stack direction="row" spacing={1} sx={{ mt: .4, color: 'text.secondary' }}><Typography variant="caption">{formatDate(task.dueOn)}</Typography>{task.assigneeName && <Typography variant="caption"><PersonOutlineRounded sx={{ fontSize: 13, verticalAlign: 'middle' }} /> {task.assigneeName}</Typography>}<Typography variant="caption"><ChatBubbleOutlineRounded sx={{ fontSize: 13, verticalAlign: 'middle' }} /> {task.checklistCompleted}/{task.checklistTotal}</Typography></Stack></Box><Chip label={taskStatusLabels[task.status] ?? task.status} color={statusColor(task.status)} size="small" /></Box>)}</Card><TaskFormDialog open={formOpen} onClose={() => setFormOpen(false)} organizationId={organizationId} dossierId={dossierId} /><TaskDetailDialog task={selected} open={Boolean(selected)} onClose={() => setSelectedId(null)} organizationId={organizationId} dossierId={dossierId} assignments={assignments} canManage={canManage && !archived} canAssign={canAssign && !archived} canValidate={canValidate && !archived} /></>;
}
